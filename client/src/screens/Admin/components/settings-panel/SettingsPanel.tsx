import { Box, Typography } from '@mui/material';

export const SettingsPanel = ({}) => (
    <Box
        style={{ maxWidth: 'lg', width: '100%', padding: '2%', height: '90vh' }}
        display={'flex'}
        justifyContent={'center'}
        alignItems={'center'}
    >
        <Typography variant="h4">Soon Will Be Available</Typography>
    </Box>
);
